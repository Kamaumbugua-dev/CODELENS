{
  "installCommand": "npm --prefix codelens install",
  "buildCommand": "npm --prefix codelens run build",
  "outputDirectory": "codelens/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
